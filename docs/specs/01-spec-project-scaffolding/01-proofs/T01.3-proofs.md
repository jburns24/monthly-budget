# T01.3 Proof Summary: FastAPI App with Health and Metrics Endpoints

## Task
Create `backend/app/main.py` and `backend/app/routers/health.py` implementing the FastAPI application entry point with health check and Prometheus metrics endpoints.

## Files Created
- `backend/app/main.py` — FastAPI application instance with lifespan handler, structlog configuration on startup, Prometheus instrumentation, and health router registration.
- `backend/app/routers/health.py` — Health check router exposing `GET /api/health` (liveness) and `GET /api/health/ready` (readiness), both checking database and/or Redis connectivity.

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T01.3-01-cli.txt | CLI import + route list | PASS |
| T01.3-02-cli.txt | Route details + lifespan + prometheus | PASS |

## Verification

- `uv run python -c "from app.main import app"` exits 0 — primary acceptance criterion met.
- All seven expected routes registered: `/openapi.json`, `/docs`, `/docs/oauth2-redirect`, `/redoc`, `/metrics`, `/api/health`, `/api/health/ready`.
- `ruff check` passes with no remaining issues on both files.
- Lifespan context manager handles startup logging and DB engine disposal on shutdown.
- `configure_logging()` called at module level so all startup messages use structlog.
- Redis connectivity check implemented using `redis.asyncio` with a 2-second connect timeout.
- Health endpoints return `{"status": "healthy"|"degraded", "database": ..., "redis": ...}` with appropriate HTTP status codes (200 / 503).
