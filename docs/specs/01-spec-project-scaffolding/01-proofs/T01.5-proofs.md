# T01.5 Proof Summary

**Task**: T01.5 - Create backend Dockerfile and placeholder test
**Status**: COMPLETED
**Date**: 2026-03-22

## Artifacts Created

| File | Description |
|------|-------------|
| `backend/Dockerfile` | Multi-stage Docker build (builder + runtime), non-root user, HEALTHCHECK, ENTRYPOINT |
| `backend/entrypoint.sh` | Shell script running alembic migrations then gunicorn |
| `backend/tests/test_placeholder.py` | Two passing pytest tests |

## Proof Results

| # | File | Type | Status |
|---|------|------|--------|
| 1 | T01.5-01-test.txt | test | PASS |
| 2 | T01.5-02-file.txt | file | PASS |

## Details

### Dockerfile
- **Builder stage**: `python:3.12-slim`, installs uv, copies `pyproject.toml` and `uv.lock`, runs `uv sync --frozen --no-install-project --no-dev`
- **Runtime stage**: `python:3.12-slim`, creates `appgroup`/`appuser` (uid/gid 1001), copies `.venv` from builder, copies app code, sets `ENV PATH`, switches to `USER appuser`, `EXPOSE 8000`
- **HEALTHCHECK**: Uses Python `urllib.request` to probe `/api/health`
- **ENTRYPOINT**: `["./entrypoint.sh"]`

### entrypoint.sh
- Runs `alembic upgrade head` before starting the server
- Starts `gunicorn` with `uvicorn.workers.UvicornWorker`
- Configurable workers via `GUNICORN_WORKERS` env var (default 4)
- Configurable port via `PORT` env var (default 8000)

### Tests
- `test_app_importable`: imports `app.main.app` and checks title
- `test_health_endpoint_schema`: mocks DB and Redis checks, calls `/api/health` via `httpx.AsyncClient`, asserts response shape

### Verification Command
```
cd backend && uv run pytest
```
Result: 2 passed
