# T02.5 Proof of Completion

## Task
Write backend auth endpoint tests

## Summary
Created `backend/tests/test_auth.py` (10 tests) and `backend/tests/test_users.py` (8 tests). All 18 tests pass. Tests use dependency_overrides for clean isolation and NullPool sessions for DB-dependent tests.

## Proof Artifacts

### 1. T02.5-01-test.txt
**Status**: PASS
**Description**: `uv run pytest tests/test_auth.py tests/test_users.py -v` — all 18 tests pass in 0.30s.

## Files Created
1. `backend/tests/test_auth.py` — 10 tests covering callback, refresh, logout
2. `backend/tests/test_users.py` — 8 tests covering GET/PUT /api/me

## Test Coverage
**test_auth.py** (10 tests):
- `test_callback_success_new_user` — mocked Google, new user, cookies set
- `test_callback_returning_user` — mocked Google, existing user, is_new_user=False
- `test_callback_google_failure_returns_401` — Google error → 401
- `test_callback_missing_body_returns_422` — missing fields → 422
- `test_refresh_no_cookie_returns_401` — no cookie → 401
- `test_refresh_valid_token_returns_new_access_cookie` — valid token → new cookie
- `test_refresh_blacklisted_token_returns_401` — blacklisted jti → 401
- `test_refresh_expired_token_returns_401` — expired token → 401
- `test_logout_no_cookie_returns_200` — always succeeds
- `test_logout_with_valid_token_returns_200_and_clears_cookies` — clears both cookies

**test_users.py** (8 tests):
- `test_me_no_cookie_returns_401` — 401 without cookie
- `test_me_authenticated_returns_user_profile` — 200 with correct profile
- `test_me_response_schema` — all required fields present
- `test_me_update_no_cookie_returns_401` — 401 without cookie
- `test_me_update_display_name` — updates display_name
- `test_me_update_timezone` — updates timezone
- `test_me_update_both_fields` — updates both fields
- `test_me_update_empty_body_no_change` — empty body, values unchanged

## Verification
- [x] All 18 tests pass
- [x] Ruff lint: all checks passed
- [x] OAuth callback mocked with AsyncMock
- [x] Expired/blacklisted token → 401
- [x] get_current_user override pattern used for user endpoint isolation

## Timestamp
Generated: 2026-03-23T00:00:00Z
