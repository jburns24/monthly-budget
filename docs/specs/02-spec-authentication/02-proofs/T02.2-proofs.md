# T02.2 Proof of Completion

## Task
Implement JWT service and get_current_user dependency

## Summary
Created `backend/app/services/jwt_service.py` (JWT creation/decoding) and `backend/app/dependencies.py` (`get_current_user` FastAPI dependency). Both pass lint, type checks, and a full JWT round-trip test.

## Proof Artifacts

### 1. T02.2-01-jwt-roundtrip.txt
**Status**: PASS
**Description**: JWT round-trip test verifying `create_access_token`, `create_refresh_token`, and `decode_token` all work correctly. Verified:
- `sub` maps to `user.google_id`
- `user_id` maps to `str(user.id)`
- `jti` is present and unique across tokens
- `exp` is set

### 2. T02.2-02-lint-typecheck.txt
**Status**: PASS
**Description**: Ruff lint and mypy type checking both pass with no errors on the two new files.

## Files Created
1. `backend/app/services/jwt_service.py` — `create_access_token`, `create_refresh_token`, `decode_token`
2. `backend/app/dependencies.py` — `get_current_user` FastAPI dependency

## Implementation Notes
- Access token: HS256, 15-minute expiry, payload: `sub`/`user_id`/`iat`/`exp`/`jti`
- Refresh token: same structure, 7-day expiry
- `get_current_user`: reads `access_token` cookie, decodes via `decode_token`, fetches User from DB
- 401 errors: verbose detail in development, generic in production
- structlog logging for all auth events (created, expired, invalid, not_found)

## Verification
- [x] JWT round-trip (create + decode) passes
- [x] Ruff lint: all checks passed
- [x] Mypy: no issues found
- [x] `get_current_user` returns 401 on missing cookie
- [x] `get_current_user` returns 401 on expired/invalid token
- [x] `get_current_user` returns 401 on user not found
- [x] Environment-dependent error detail (verbose dev, generic prod)

## Timestamp
Generated: 2026-03-23T00:00:00Z
