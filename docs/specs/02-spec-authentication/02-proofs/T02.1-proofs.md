# T02.1 Proof Summary — Add auth dependencies and config validation

**Task:** T02.1 — Add auth dependencies and config validation
**Spec:** docs/specs/02-spec-authentication/02-spec-authentication.md (Unit 2)
**Completed:** 2026-03-23

## Artifacts

| File | Type | Status |
|------|------|--------|
| T02.1-01-uv-sync.txt | cli | PASS |
| T02.1-02-imports.txt | cli | PASS |
| T02.1-03-config-validation.txt | cli | PASS |

## Changes Made

### `backend/pyproject.toml`
- Added `PyJWT>=2.8.0` and `google-auth>=2.25.0` to dependencies

### `backend/app/config.py`
- Added `@model_validator(mode="after")` `validate_auth_secrets` that raises `ValueError` when:
  - `jwt_secret` is empty or < 32 chars in non-development/non-test environments
  - `google_client_id` is empty in non-development/non-test environments
- Skips validation when `ENVIRONMENT` is `development` or `test`

### `backend/app/main.py`
- Added `CORSMiddleware` with `allow_credentials=True`
- In development: `allow_origins=["http://localhost:5173"]`
- In non-development: empty origins list

### `backend/app/schemas/auth.py` (new)
- `LoginCallbackRequest` — code + code_verifier
- `LoginCallbackResponse` — is_new_user
- `TokenError` — detail

### `backend/app/schemas/user.py` (new)
- `UserResponse` — id, email, display_name, avatar_url, timezone
- `UserUpdate` — optional display_name, timezone

## Proof Results

- `uv sync --all-extras` resolves 45 packages successfully
- `import jwt` → version 2.12.1 ✓
- `import google.auth` → version 2.49.1 ✓
- Config validation fails fast with `ValidationError` for bad JWT_SECRET in production ✓
- Config validation fails fast for empty `google_client_id` in production ✓
- Config validation is skipped in development environment ✓
- All existing tests still pass (2 passed) ✓
