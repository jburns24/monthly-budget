# T03.4 Proof Summary — receipt_service.process_upload (three-phase transaction)

**Task**: T03.4 — receipt_service.process_upload (three-phase transaction)
**Date**: 2026-04-19
**Status**: COMPLETED

## Artifacts

| File | Type | Status | Description |
|------|------|--------|-------------|
| T03.4-01-test.txt | test | PASS | 19/19 tests pass |
| T03.4-02-cli.txt | cli | PASS | ruff clean + imports verified |

## Summary

`process_upload` implemented in `backend/app/services/receipt_service.py` with:

- **Pre-phase**: MIME validation + image sanitization (pure compute, no side effects)
- **Phase 1** (atomic savepoint): save image to disk + insert `Receipt(status="processing")`
- **Phase 2** (non-atomic): call `claude_client.extract_receipt`; handle API errors and non-receipts
- **Phase 3** (atomic savepoint): update receipt fields + create `Expense` with suggested category

On Phase 2/3 failure: `_mark_failed()` sets `receipt.status="failed"`, deletes image file.

Returns `(receipt, expense_or_None, needs_edit)` where `needs_edit=True` when confidence is low or no total extracted.

## Test Coverage (19 tests, 0.69s)

- Pre-phase: 2 tests (invalid MIME → 422, corrupt image → 422)
- Phase 1: 1 test (save failure → 500, no image delete)
- Phase 2: 2 tests (Claude error → 503, non-receipt → 422; both mark failed + delete image)
- Phase 3 happy paths: 4 tests (high confidence+expense, low confidence+needs_edit, no category, no total)
- Helper units: 10 tests (_parse_expense_date × 4, _amount_cents × 6)

## Files Modified/Created

- `backend/app/services/receipt_service.py` — new service with process_upload + helpers
- `backend/tests/test_receipt_service.py` — 19 new tests
