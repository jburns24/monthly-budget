# T04.4 Proof Summary — ReceiptCaptureDialog Component

**Task**: T04.4 — ReceiptCaptureDialog component (Phase state machine)
**Status**: COMPLETED
**Date**: 2026-04-19

## Files Created

`frontend/src/components/expenses/ReceiptCaptureDialog.tsx` — Phase state machine dialog:

| Feature | Detail |
|---------|--------|
| Phase enum | `'idle' \| 'preview' \| 'uploading' \| 'reviewing' \| 'done'` via `useReducer` |
| File input | react-dropzone with `capture="environment"`, accept jpeg/png/webp/heic/heif, 5MB max |
| Compression | `browser-image-compression` in mutation fn (1.5MB, 2400px) |
| Upload | TanStack `useMutation` — onSuccess dispatches UPLOAD_SUCCESS + invalidates 3 query keys |
| Error mapping | `ERROR_MESSAGES` record: 422/429/503/413/415 → user-facing toasts |
| Review UI | merchant, amount (cents→dollars), NativeSelectRoot category override, needs-review badge |
| data-testids | receipt-dropzone, receipt-file-input, receipt-preview-image, receipt-uploading, receipt-reviewing, receipt-needs-review-badge, receipt-merchant, receipt-amount, receipt-category-select, receipt-done, receipt-back-btn, receipt-upload-btn, receipt-confirm-btn, receipt-close-btn |

`frontend/src/__tests__/ReceiptCaptureDialog.test.tsx` — 22 tests, one describe per Phase:

| Phase | Tests |
|-------|-------|
| idle | dialog renders/closed, dropzone text, capture attr, Cancel button |
| preview | image shown, file name, Back/Upload buttons, Back resets to idle |
| uploading | spinner visible, error returns to preview, 422 toast description |
| reviewing | merchant/amount display, null formatting, needs-review badge, category select, query invalidation |
| done | success message, Close button, Close calls onOpenChange(false) |

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T04.4-01-test.txt | vitest (187 tests) | PASS |
| T04.4-02-tsc.txt | tsc --noEmit | PASS |
