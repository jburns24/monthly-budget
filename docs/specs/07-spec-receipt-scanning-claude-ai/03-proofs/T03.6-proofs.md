# T03.6 Proof Summary — expense cascade-delete of linked receipt

**Task:** T03.6
**Status:** COMPLETED
**Date:** 2026-04-19
**Model:** sonnet

## Implementation

Modified `backend/app/services/expense_service.py`:

- Added `from app.services import receipt_storage` import
- Updated `delete_expense` to use `selectinload(Expense.receipt)` so the linked Receipt is eager-loaded
- If `expense.receipt_id is not None and expense.receipt is not None`:
  - Calls `await receipt_storage.delete(Path(linked_receipt.image_path))` to remove the on-disk image
  - Calls `await db.delete(linked_receipt)` to remove the Receipt row from the database
- Then deletes the Expense row and flushes as before (all in the same transaction)

Modified `backend/tests/test_expenses_api.py`:

- Added `from app.models.receipt import Receipt` (top-level, registers with Base.metadata)
- Added `create_test_receipt` to conftest imports
- Added `test_delete_expense_cascades_to_receipt` covering the cascade-delete behavior

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T03.6-01-test.txt | test | PASS — cascade-delete test passes |
| T03.6-02-file.txt | file | PASS — implementation and test files verified |

## Test Coverage (1 new test)

**Cascade-delete:**
1. `test_delete_expense_cascades_to_receipt` — verifies DELETE expense removes linked Receipt row and calls receipt_storage.delete with the correct image path

**Regression check:** All 20 tests in test_expenses_api.py pass (19 pre-existing + 1 new).
