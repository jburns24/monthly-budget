# T03.2 Proof Summary — rate_limiter service (Redis pipeline, fail-open)

**Task:** T03.2
**Status:** COMPLETED
**Date:** 2026-04-19
**Model:** sonnet

## Implementation

Created `backend/app/services/rate_limiter.py` exposing:

```python
async def check_and_increment_receipt_upload(
    family_id: uuid.UUID,
    limit: int = 50,
) -> tuple[bool, int]:
```

Key behaviors:
- Redis key: `receipts:uploads:{family_id}:{YYYY-MM-DD}`
- Pipeline: `INCR` + `EXPIRE 86400` + `TTL` (atomic)
- Returns `(count <= limit, count)` on success
- Returns `(True, 0)` on any Redis error (fail-open), logs warning

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T03.2-01-test.txt | test | PASS — 9/9 tests pass |
| T03.2-02-file.txt | file | PASS — implementation file verified |

## Test Coverage

9 tests in `backend/tests/test_rate_limiter.py`:
1. `test_allows_first_upload` — count=1 ≤ limit=50 → allowed
2. `test_allows_upload_at_limit_minus_one` — count=49 ≤ 50 → allowed
3. `test_allows_upload_at_exact_limit` — count=50 = 50 → allowed
4. `test_blocks_upload_over_limit` — count=51 > 50 → blocked
5. `test_fails_open_on_redis_error` — pipeline() raises → (True, 0)
6. `test_fails_open_on_pipeline_execute_error` — execute() raises → (True, 0)
7. `test_uses_correct_redis_key_format` — key includes family_id + today's date
8. `test_pipeline_sets_expire_on_key` — EXPIRE called with 86400
9. `test_default_limit_is_50` — no limit arg, count=51 → blocked
