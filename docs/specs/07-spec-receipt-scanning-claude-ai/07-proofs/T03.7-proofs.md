# T03.7 Proof Summary — Infra wiring (Dockerfile libmagic, Tiltfile mount, config, env)

**Task:** T03.7 (task ID #17)
**Date:** 2026-04-19
**Status:** COMPLETED

## Artifacts

| File | Type | Status | Description |
|------|------|--------|-------------|
| T03.7-01-file.txt | file | PASS | libmagic1 installed in Dockerfile dev + runtime stages |
| T03.7-02-file.txt | file | PASS | Tiltfile: mkdir, volume mount, and 3 env vars present |
| T03.7-03-cli.txt | cli | PASS | config.py Settings.receipt_storage_path: Path = /data/receipts |
| T03.7-04-file.txt | file | PASS | .env.example files contain ANTHROPIC_MOCK, ANTHROPIC_MOCK_SCENARIO, RECEIPT_STORAGE_PATH |

## Changes Made

### backend/Dockerfile
- Added `RUN apt-get update && apt-get install -y --no-install-recommends libmagic1 && rm -rf /var/lib/apt/lists/*` to **dev stage** (required since Tiltfile uses `--target dev`)
- Added same apt-get install to **runtime stage** (per spec requirement)

### Tiltfile
- Added `local('mkdir -p backend/data/receipts', echo_off=True)` before `api` local_resource
- Added `-v $(pwd)/backend/data/receipts:/data/receipts` to api container serve_cmd
- Added `ANTHROPIC_MOCK`, `ANTHROPIC_MOCK_SCENARIO`, `RECEIPT_STORAGE_PATH` to `api_env`

### backend/app/config.py
- Added `receipt_storage_path: Path = Field(default=Path("/data/receipts"))` to Settings class

### backend/.env.example + .env.example (root)
- Added `ANTHROPIC_MOCK=false`, `ANTHROPIC_MOCK_SCENARIO=success`, `RECEIPT_STORAGE_PATH=/data/receipts`

### .gitignore
- Added `backend/data/` to prevent local receipt uploads from being committed
