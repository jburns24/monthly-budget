# T02.4 Proof Summary — Dev-only mock-claude toggle route

**Task:** T02.4 (task ID #10)
**Date:** 2026-04-19
**Status:** COMPLETED

## Artifacts

| File | Type | Status | Description |
|------|------|--------|-------------|
| T02.4-01-test.txt | test | PASS | 7/7 dev_receipts router tests pass including production 404 gate |
| T02.4-02-file.txt | file | PASS | Router registered in main.py, POST /api/dev/mock-claude defined correctly |

## Changes Made

### backend/app/routers/dev_receipts.py (new file)
- `POST /api/dev/mock-claude?scenario=...` endpoint
- Internal env gate: raises `HTTP 404` unless `settings.environment in ("development", "test")`
- Updates `settings.anthropic_mock_scenario` in-process and returns `{"scenario": "..."}`
- Default scenario is `"success"` when no query param provided

### backend/app/main.py (modified)
- Added `dev_receipts` to conditional router registration block alongside `dev_auth`
- Both routers only registered when `settings.environment in ("development", "test")`

### backend/tests/test_dev_receipts_router.py (new file)
- 7 tests: happy path, in-process mutation, default param, all 5 scenarios, production 404, staging 404, test-env 200
- Uses `patch.object(settings, "environment", "production")` to verify the env gate
