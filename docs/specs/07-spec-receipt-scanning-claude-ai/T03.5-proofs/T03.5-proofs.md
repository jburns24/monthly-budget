# T03.5 Proof Summary — routers/receipts.py

**Task**: T03.5 — All 6 receipt endpoints + error mapping + 5MB cap
**Status**: COMPLETED
**Date**: 2026-04-20

## Files Created/Modified

- `backend/app/routers/receipts.py` — 6 endpoints: POST upload, GET list, GET single, GET image, POST retry, DELETE
- `backend/app/services/receipt_service.py` — added `reprocess_receipt`, `_run_phase3` helper; fixed error codes (415/400); image preserved on Claude errors
- `backend/app/main.py` — registered `receipts.router`
- `backend/tests/test_receipts_api.py` — 19 endpoint tests with dependency_overrides pattern
- `backend/alembic/versions/c4e2f1b3d5a8_add_receipts_and_pg_trgm.py` — added orphan-cleanup step before FK constraint

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T03.5-01-test.txt | test | PASS |
| T03.5-02-lint.txt | lint | PASS |

## Test Summary

- **38 tests passed** (19 API endpoint tests + 19 service unit tests)
- All 6 endpoints covered: upload, list, get, image stream, retry, delete
- Error codes verified: 201, 204, 400, 403, 404, 409, 410, 413, 415, 422, 429, 500, 503
- 5MB cap enforced; rate-limit 429 with `Retry-After` header
- `StreamingResponse` for image with `Cache-Control: private, max-age=3600`
- RBAC: delete restricted to uploader or family admin
- Retry rejects completed receipts with 409
