# T04.1 Proof Summary — Frontend types (receipts + expenses extension)

**Task**: T04.1 — Create receipts.ts types; extend expenses.ts Expense
**Status**: COMPLETED
**Date**: 2026-04-20

## Files Created/Modified

- `frontend/src/types/receipts.ts` — new file: `ReceiptStatus`, `Receipt`, `ReceiptUploadResponse`
- `frontend/src/types/expenses.ts` — added `receipt_id: string | null` and `receipt_status: ReceiptStatus | null` to `Expense`

## Type Shapes

### receipts.ts
```ts
export type ReceiptStatus = 'processing' | 'completed' | 'failed'

export interface Receipt {
  id: string
  family_id: string
  uploaded_by: string
  image_path: string | null
  raw_response: Record<string, unknown> | null
  parsed_date: string | null
  parsed_total_cents: number | null
  parsed_merchant: string | null
  status: ReceiptStatus
  error_message: string | null
  created_at: string
}

export interface ReceiptUploadResponse {
  receipt: Receipt
  expense_id: string | null
  needs_edit: boolean
}
```

### expenses.ts additions
```ts
receipt_id: string | null
receipt_status: ReceiptStatus | null
```

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T04.1-01-tsc.txt | tsc --noEmit | PASS |
| T04.1-02-test.txt | vitest (165 tests) | PASS |
