# T02.3 Proof Summary — Mock branch with 5 deterministic scenarios

**Task**: T02.3 — Mock branch with 5 deterministic scenarios
**Date**: 2026-04-19
**Status**: COMPLETED

## Artifacts

| File | Type | Status | Description |
|------|------|--------|-------------|
| T02.3-01-test.txt | test | PASS | 19/19 tests pass (7 new mock + 12 existing T02.2) |

## Summary

All 7 new tests in `tests/test_claude_mock.py` pass in 0.22s:
- `test_mock_success_scenario` — is_receipt=True, high, total=42.50, date=2026-03-21 — PASS
- `test_mock_medium_confidence_scenario` — is_receipt=True, medium, no date — PASS
- `test_mock_low_confidence_scenario` — is_receipt=True, low, no fields — PASS
- `test_mock_non_receipt_scenario` — is_receipt=False — PASS
- `test_mock_api_error_scenario` — raises APIStatusError(503) without calling real client — PASS
- `test_mock_disabled_calls_real_client` — mock=False routes to real client — PASS
- `test_mock_unknown_scenario_falls_back_to_success` — unknown scenario → success — PASS

All 12 existing T02.2 tests still pass (no regression).

## Files Modified/Created

- `backend/app/services/claude_client.py` — refactored to separate:
  - `_call_claude()` (internal, `@_retry`-decorated, real API path)
  - `extract_receipt()` (public, checks `settings.anthropic_mock` first, no tenacity on mock path)
  - `_MOCK_SCENARIOS` dict with 4 deterministic ExtractedReceipt objects
  - `_get_mock_response(scenario)` helper — raises APIStatusError(503) for 'api_error'
- `backend/tests/test_claude_mock.py` — 7 new tests for mock scenarios
