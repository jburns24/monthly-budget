# T06 Proof Summary — Observability & E2E Test Suite

**Task:** T06 (task ID #6)
**Date:** 2026-04-20
**Status:** COMPLETED

## Artifacts

| File | Type | Status | Description |
|------|------|--------|-------------|
| T06-01-test.txt | test | PASS | 11/11 backend unit tests pass (metrics + middleware) |
| T06-02-file.txt | file | PASS | All implementation files exist and wired correctly |

## Changes Made

### backend/app/metrics.py (new file)
- `receipts_processed_total{status}` Counter — labels: `completed`, `failed`
- `receipts_upload_errors_total{error_type}` Counter — labels: `non_receipt`, `invalid_format`, `rate_limited`, `api_error`, `timeout`, `too_large`
- `claude_api_latency_seconds` Histogram — buckets: `0.5, 1, 2, 5, 10, 30`
- `receipt_image_bytes` Histogram — buckets: `102400, 262144, 524288, 1048576, 2097152, 5242880`

### backend/app/middleware/request_id.py (new file)
- `RequestIDMiddleware` reads `X-Request-ID` or generates UUID4
- Binds to structlog contextvars (`bind_contextvars(request_id=...)`)
- Echoes on response header, clears contextvars on completion

### backend/app/middleware/__init__.py (new file)
- Empty package init

### backend/app/logging.py (modified)
- Added `_SENSITIVE_KEYS` frozenset: `{anthropic_api_key, jwt_secret, google_client_secret, password, authorization}`
- Added `_sensitive_filter` processor as the **first** entry in `shared_processors`
- Redacts matching keys to `"[REDACTED]"`

### backend/app/main.py (modified)
- Imported `RequestIDMiddleware` from `app.middleware.request_id`
- Registered `app.add_middleware(RequestIDMiddleware)` after CORS middleware, before Prometheus

### e2e/fixtures/sample-receipt.jpg (new file)
- Real JPEG, 26,804 bytes (~26KB), generated by Pillow
- Depicts a mock Whole Foods receipt with store name, items, and total

### e2e/fixtures/create-sample-receipt.py (new file)
- One-off Pillow generator script for `sample-receipt.jpg`
- Run: `python e2e/fixtures/create-sample-receipt.py`

### e2e/fixtures/test-data.ts (modified)
- Added `setMockScenario(request, scenario)` helper — POSTs to `/api/dev/mock-claude?scenario=...`

### e2e/tests/receipts.spec.ts (new file)
- 5 scenarios tested under `ANTHROPIC_MOCK=true`:
  - (a) `success` — uploading shows reviewing state with merchant + amount
  - (b) `non_receipt` — 422 → "doesn't look like a receipt" error toast
  - (c) `low_confidence` — "Needs review" badge shown in reviewing state
  - (d) `api_error` — 503 → "Receipt service unavailable" error toast
  - (e) multi-user — User A uploads via API, User B sees expense via API

### e2e/playwright.config.ts (modified)
- Added `video: 'retain-on-failure'` for post-failure debugging

### backend/tests/test_metrics.py (new file)
- 6 tests: Counter increments, Histogram observations, registry discovery

### backend/tests/test_request_id_middleware.py (new file)
- 5 tests: echo round-trip, UUID generation, uniqueness, SensitiveFilter redaction
