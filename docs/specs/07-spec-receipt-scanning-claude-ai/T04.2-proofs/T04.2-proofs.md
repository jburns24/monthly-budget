# T04.2 Proof Summary — Frontend api/receipts.ts

**Task**: T04.2 — api/receipts.ts (upload + CRUD helpers)
**Status**: COMPLETED
**Date**: 2026-04-20

## File Created

`frontend/src/api/receipts.ts` — 5 exported functions:

| Function | Method | Notes |
|----------|--------|-------|
| `uploadReceipt(familyId, file)` | POST multipart | FormData, no explicit Content-Type; throws `new Error(status)` for non-OK |
| `getReceipts(familyId, params?)` | GET | Optional filters: status, uploaded_by, date_from, date_to, page, per_page |
| `getReceipt(familyId, receiptId)` | GET | Single receipt by ID |
| `deleteReceipt(familyId, receiptId)` | DELETE | Returns void |
| `retryReceipt(familyId, receiptId)` | POST | Throws `new Error(status)` for non-OK (409 handled by dialog) |

## Key Design Note

`uploadReceipt` and `retryReceipt` throw `new Error(String(response.status))` on failure,
allowing the ReceiptCaptureDialog to map 422/429/503/413/415 to specific user-facing toasts.

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T04.2-01-tsc.txt | tsc --noEmit | PASS |
| T04.2-02-test.txt | vitest (165 tests) | PASS |
