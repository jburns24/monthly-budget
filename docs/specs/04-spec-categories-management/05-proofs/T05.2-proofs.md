# T05.2 Proof Summary: Write Playwright E2E Tests for Full Category Lifecycle

## Task
Write `e2e/tests/categories.spec.ts` covering the complete category lifecycle in Playwright.

## Artifacts

| File | Type | Status |
|------|------|--------|
| T05.2-01-test.txt | Test run (6 tests) | PASS |
| T05.2-02-cli.txt | Test list + notes | PASS |

## Test Results

All 6 tests pass:

1. **admin creates a category and sees it in the list** - PASS (614ms)
2. **admin edits a category name and sees the updated name** - PASS (712ms)
3. **admin deletes a category with no expenses and it disappears** - PASS (746ms)
4. **seed defaults creates 6 default categories** - PASS (440ms)
5. **member can view categories but cannot see admin action buttons** - PASS (337ms)

Setup test also passes: `authenticate primary test user`

## Files Created/Modified

- **Created**: `e2e/tests/categories.spec.ts`
- **Fixed**: `frontend/src/api/categories.ts` — changed `method: 'PATCH'` to `method: 'PUT'` to match the backend `PUT /api/families/{id}/categories/{id}` endpoint (was causing 405 Method Not Allowed)

## Implementation Notes

- Used Page Object Model (`CategoriesPage`) throughout
- `beforeEach` resets test data and creates User A + family
- Each test creates its own API context for setup, then disposes
- Invite accept flow: uses `GET /api/invites` to retrieve invite ID (since `POST /api/families/{id}/invites` returns `GenericMessage` not the invite object)
- Member session switching done via `addCookies` following the pattern in `invite.spec.ts`
- `waitForResponse` used to verify API calls (create: POST 201, edit: PUT 200, delete: DELETE 200, seed: POST 200)

## Commit SHA
To be filled after commit
