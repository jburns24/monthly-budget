# T03.3 Proof Summary

Task: Add create_test_category factory and write API endpoint tests

## Artifacts

| File | Type | Status |
|------|------|--------|
| T03.3-01-test.txt | pytest run | PASS |
| T03.3-02-lint.txt | ruff lint + format check | PASS |

## Results

- **16/16 tests passed** in `backend/tests/test_categories_api.py`
- **Lint clean**: ruff check + format check both pass

## Files Modified / Created

- `backend/tests/conftest.py` — added `create_test_category` factory function
- `backend/tests/test_categories_api.py` — created with 16 API endpoint tests covering:
  - GET list: active categories returned, non-members get 404, empty list case
  - POST create: admin gets 201, non-admin gets 403, duplicate name gets 409
  - PUT update: admin updates fields, 404 for missing, 403 for non-admin
  - DELETE: hard-delete confirmed in DB, 403 for non-admin, 404 for missing
  - POST seed: creates 6 defaults, idempotent (second call = 0), 403 for non-admin, 404 for non-member
