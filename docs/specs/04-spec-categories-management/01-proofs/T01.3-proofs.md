# T01.3 Proof Summary

Task: Add categories to test_reset truncation order and write model unit tests

## Artifacts

| File | Type | Status |
|------|------|--------|
| T01.3-01-test.txt | pytest | PASS |
| T01.3-02-cli.txt | ruff lint | PASS |

## Implementation Summary

### Part 1: dev_auth.py — test_reset truncation order
Added `await db.execute(text("DELETE FROM categories"))` before the `DELETE FROM families` line in the `test_reset` endpoint. Categories have a FK to families (CASCADE ondelete), so they must be deleted before families to avoid FK constraint violations.

Updated the docstring to reflect the new order:
`invites → family_members → categories → families → users → refresh_token_blacklist`

### Part 2: test_categories_models.py — 9 unit tests
All tests pass against the live database:

1. `test_create_and_retrieve_category` — all fields persisted correctly
2. `test_category_is_active_defaults_to_true` — server-side default verified
3. `test_category_sort_order_defaults_to_zero` — default 0 verified
4. `test_category_id_auto_generated` — UUID auto-generation verified
5. `test_category_unique_constraint_raises_on_duplicate` — IntegrityError on duplicate (family_id, name)
6. `test_category_unique_constraint_allows_same_name_different_family` — same name allowed across families
7. `test_category_cascade_delete_with_family` — categories deleted when family is deleted
8. `test_category_icon_is_nullable` — icon field is nullable
9. `test_category_family_relationship` — FK to families table verified
