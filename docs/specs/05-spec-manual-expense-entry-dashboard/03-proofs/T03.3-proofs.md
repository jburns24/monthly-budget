# T03.3 Proof Summary

## Task
Write API endpoint tests and integration tests for expenses

## Files Created
- `backend/tests/test_expenses_api.py` — 19 unit-style API endpoint tests
- `backend/tests/test_expenses_integration.py` — 4 integration/lifecycle tests

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T03.3-01-test.txt | test | PASS |
| T03.3-02-lint.txt | lint | PASS |

## Test Coverage

### test_expenses_api.py (19 tests)
- POST creates expense for member (201)
- POST validates amount > 0 (422) — zero and negative cases
- POST validates inactive category (400)
- POST non-member returns 404
- GET lists expenses by month
- GET paginates correctly
- GET filters by category
- GET non-member returns 404
- GET single expense returns 200
- GET single expense not found returns 404
- PUT updates fields (200)
- PUT returns 409 on stale updated_at
- PUT not found returns 404
- DELETE removes expense record
- DELETE not found returns 404
- DELETE non-member returns 404
- GET budget summary returns correct aggregation
- GET budget summary non-member returns 404

### test_expenses_integration.py (4 tests)
- Full expense lifecycle (create -> list -> update -> get -> delete -> list empty)
- Budget summary with multiple categories and expenses (green/yellow/none status)
- RBAC: member can CRUD expenses
- RBAC: non-member gets 404 on all expense endpoints

## All 23 tests: PASS
