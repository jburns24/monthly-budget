# T01.2 Proof Summary

**Task**: Generate Alembic migration for expenses and monthly_goals tables
**Status**: COMPLETED
**Date**: 2026-04-04

## Artifacts

| File | Type | Status | Description |
|------|------|--------|-------------|
| T01.2-01-cli.txt | cli | PASS | alembic upgrade head - migration applies successfully |
| T01.2-02-cli.txt | cli | PASS | alembic downgrade -1 + re-upgrade - reversible migration verified |

## Migration Details

- **Revision ID**: ab32c81d8481
- **Previous Revision**: 587bc20d2058 (add categories table)
- **File**: `backend/alembic/versions/ab32c81d8481_add_expenses_and_monthly_goals_tables.py`

### Tables Created

**expenses**:
- `id` UUID PK
- `family_id` UUID FK -> families.id CASCADE
- `category_id` UUID FK -> categories.id RESTRICT
- `user_id` UUID FK -> users.id RESTRICT
- `amount_cents` INTEGER NOT NULL
- `description` VARCHAR(500) NOT NULL
- `expense_date` DATE nullable
- `year_month` VARCHAR(7) nullable
- `receipt_id` UUID nullable
- `created_at`, `updated_at` TIMESTAMP WITH TIME ZONE

**monthly_goals**:
- `id` UUID PK
- `family_id` UUID FK -> families.id CASCADE
- `category_id` UUID FK -> categories.id CASCADE
- `year_month` VARCHAR(7) NOT NULL
- `amount_cents` INTEGER nullable
- `version` INTEGER NOT NULL
- `created_at`, `updated_at` TIMESTAMP WITH TIME ZONE

### Indexes Created

- `idx_expenses_category` on expenses(category_id)
- `idx_expenses_family` on expenses(family_id)
- `idx_expenses_family_year_month` on expenses(family_id, year_month)
- `idx_monthly_goals_family_year_month` on monthly_goals(family_id, year_month)

### Constraints

- `ck_expenses_amount_positive`: amount_cents > 0
- `ck_monthly_goals_amount_positive`: amount_cents > 0
- `uq_monthly_goals_family_category_month`: UNIQUE(family_id, category_id, year_month)
