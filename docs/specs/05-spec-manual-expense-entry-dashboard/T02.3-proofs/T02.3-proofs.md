# T02.3 Proof Summary

**Task**: Update _count_category_expenses to query real Expense model
**Status**: COMPLETED

## Changes Made

### `backend/app/services/category_service.py`
- Added `func` to sqlalchemy imports
- Added `from app.models.expense import Expense` import
- Replaced stub `_count_category_expenses()` (returned 0) with real query:
  `select(func.count()).select_from(Expense).where(Expense.category_id == category_id)`

### `backend/tests/test_categories_service.py`
- Added `from app.models.expense import Expense` import
- Added `test_delete_category_archives_when_expenses_exist` test that verifies:
  - When an expense references a category, `delete_category` returns `{"deleted": False, "archived": True, "expense_count": 1}`
  - The category's `is_active` field is set to `False`

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T02.3-01-test.txt | test | PASS |
| T02.3-02-lint.txt | cli (lint) | PASS |

## Test Results
- 24 tests passed (23 original + 1 new archive test)
- All checks pass with ruff lint and format
