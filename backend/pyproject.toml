[project]
name = "monthly-budget-backend"
version = "0.1.0"
description = "Monthly Budget Backend API"
requires-python = ">=3.12"
authors = [
    {name = "Team", email = "team@example.com"}
]

dependencies = [
    "fastapi>=0.104.0",
    "uvicorn>=0.24.0",
    "gunicorn>=21.2.0",
    "sqlalchemy[asyncio]>=2.0.0",
    "asyncpg>=0.29.0",
    "alembic>=1.13.0",
    "structlog>=24.1.0",
    "prometheus-fastapi-instrumentator>=6.1.0",
    "pydantic-settings>=2.1.0",
    "redis>=5.0.0",
    "PyJWT>=2.8.0",
    "google-auth>=2.25.0",
    "httpx>=0.25.0",
    "requests>=2.28.0",
    "email-validator>=2.1.0",
    "pillow>=10",
    "pillow-heif>=0.16",
    "python-magic>=0.4.27",
    "aiofiles>=23.2",
    "anthropic>=0.40",
    "tenacity>=8.2",
    "types-aiofiles>=25.1.0.20260409",
    "python-multipart>=0.0.26",
]

[tool.setuptools]
packages = ["app"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[dependency-groups]
# PEP 735 group, not a PEP 621 extra: `uv sync` installs default groups but
# never extras, so tooling declared here is present in CI's bare `uv sync`.
dev = [
    "mypy>=1.19.1",
    "pytest>=7.4.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.1.0",
]

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
