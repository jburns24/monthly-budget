# PostgreSQL configuration
POSTGRES_USER=monthly_budget
POSTGRES_PASSWORD=changeme_postgres_password
POSTGRES_DB=monthly_budget

# Redis configuration
REDIS_PASSWORD=changeme_redis_password

# Assembled connection URLs (used by the backend app and alembic)
DATABASE_URL=postgresql+asyncpg://monthly_budget:changeme_postgres_password@localhost:5432/monthly_budget
REDIS_URL=redis://:changeme_redis_password@localhost:6379/0

# Optional: Alembic-only connection URL. Falls back to DATABASE_URL when unset.
# Only needed against hosted Supabase's transaction pooler (see below), whose
# port 6543 rejects the named prepared statements Alembic's DDL requires --
# point this at the session pooler or direct connection instead.
# DATABASE_MIGRATION_URL=postgresql+asyncpg://monthly_budget:changeme_postgres_password@localhost:5432/monthly_budget
#
# Hosted Supabase connection strings (see docs/data-layer-ports-design.md,
# "Connecting to hosted Supabase"), one example per mode:
#
# Supavisor transaction pooler (:6543) -- app traffic only; NOT valid for
# DATABASE_MIGRATION_URL. IPv4-safe.
# DATABASE_URL=postgresql+asyncpg://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres
#
# Supavisor session pooler (:5432 on the pooler host) -- safe for both app
# traffic and migrations. IPv4-safe, so this is what CI runners without IPv6
# egress must use.
# DATABASE_URL=postgresql+asyncpg://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres
#
# Direct connection -- safe for both app traffic and migrations, but the host
# is IPv6-only unless Supabase's paid IPv4 add-on is purchased.
# DATABASE_URL=postgresql+asyncpg://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres

# Backend application secrets
SECRET_KEY=changeme_secret_key_min_32_chars_long
JWT_SECRET=changeme_jwt_secret_min_32_chars_long

# Google OAuth 2.0
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-google-client-secret

# Anthropic API (for receipt scanning)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key
ANTHROPIC_MOCK=false
ANTHROPIC_MOCK_SCENARIO=success

# Receipt storage
RECEIPT_STORAGE_PATH=/data/receipts

# Application settings
ENVIRONMENT=development
LOG_LEVEL=INFO

# Frontend settings
VITE_API_BASE_URL=http://localhost:8000

# Optional: override the GITHUB_TOKEN used for `task skills:*` (agent-skills OCI
# registry auth). Auto-derived from `gh auth token` if unset.
# GITHUB_TOKEN=
